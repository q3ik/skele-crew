{
  "name": "@skele-crew/root",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo root for skele-crew — AI agent company management infrastructure",
  "workspaces": [
    "mcp/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:memory": "npm test --workspace=mcp/memory-server"
  }
}
